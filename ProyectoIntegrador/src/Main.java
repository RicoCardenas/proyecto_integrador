import ClaseVentana.Ventana;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        Ventana v1 = new Ventana(); //Creamos el Objeto ventana v1 de la clase Ventana

        v1.setVisible(true); //Hacemos visible la ventana usando el valor booleano tue
    }
}